# 루프가 찾은 것

QA 가 **통과시키면서** 남긴 지적이다 — 기준은 만족했지만 눈에 걸린 것들.
사람이 읽고 필요한 것만 `list.md` 로 옮긴 뒤 **그 줄을 지운다.** 남은 줄 = 아직 안 본 것.
루프는 이 파일을 읽지 않는다 (세션이 앞질러 가지 않게).

**지금 남은 것은 전부 「그 단계가 와야 뜻이 있는 것」이다** (2026-09-22 사람이 훑었다) —
멀티플레이 · 건축 격자 · 농사 재설계 · 디버그 도구가 한 씬에 모이는 때.

- `09-21 16:39` **G-110.2** spec/08_combat/damage-death.md 의 「죽은 본인만 열고 꺼낼 수 있다」가 아직… ‖ spec/08_combat/damage-death.md 의 「죽은 본인만 열고 꺼낼 수 있다」가 아직 없다 — DeathChest 에 주인이 없고 Death.open 은 누구든 연다. 이번 단계 기준에는 없으므로(멀티플레이 몫) 지금 고칠 일은 아니고, 그 단계에서 기준으로 올라와야 한다
- `09-21 12:59` **G-107.2** 씨앗과 물뿌리개를 게임 안에서 얻을 길이 없다 ‖ 씨앗과 물뿌리개를 게임 안에서 얻을 길이 없다. recipes.json 에 wheat_seed 가 없고 watering_can 도 아무도 주지 않는다 — spec/04_life/farming.md 는 「씨앗은 제작대에서 나무로 만든다 (임시)」라고 적고 있다. 삽 · 도끼도 똑같이 얻을 길이 없으니 이번 단계가 만든 구멍은 아니다. 사람이 손으로 농사를 해 보려면 필요하므로 다음에 어느 단계가 맡을지 정해 두는 게 좋다.
- `09-21 11:31` **G-106.1** Stations.aimed_spot() 이 놓을 자리를 1m 칸 가운데(Grid)로 맞추면서 그 칸이… ‖ Stations.aimed_spot() 이 놓을 자리를 1m 칸 가운데(Grid)로 맞추면서 그 칸이 이미 찼는지는 안 본다 — 조준점 광선이 제작대에 맞으면만 막는다. 크기가 정확히 1m 인 제작대(cook_stove)는 같은 칸 안의 남은 땅을 겨눌 수 있어 한 칸에 둘이 겹쳐 설 수 있다. 설치 격자 규칙이 미정이라(spec/06_build/building.md) 이번 기준에는 없지만, 격자 규칙이 정해지는 단계에서 「찬 칸에는 못 놓는다」를 기준과 테스트로 같이 넣어라.
- `09-21 08:50` **G-103.5** GameRoot.set_menu_open(false) 이 씬 안 모든 Player 의 controls_… ‖ GameRoot.set_menu_open(false) 이 씬 안 모든 Player 의 controls_enabled 를 무조건 true 로 되돌린다. debug_tools.gd:65 도 같은 값을 쓰므로, 디버그 비행 카메라를 켠 채 ESC 를 열었다 닫으면 꺼 뒀던 조작이 되살아난다. 지금은 디버그 도구가 시험 섬에만 있어 부딪히지 않는다 — 두 곳이 한 씬에 모이는 단계에서 「누가 껐나」를 세는 방식(이유별 잠금)으로 바꾸고 테스트를 붙여라.

# 결정 대기

루프가 스스로 풀지 못한 것을 여기로 넘긴다. **사람은 이 파일과 `list.md` 만 고친다.**

## 어떻게 답하나
1. 아래 항목의 「사람이 답할 것」에 답을 적는다
2. `list.md` 에서 그 묶음의 `[>]` 를 `[ ]` 로 되돌린다
3. 묶음 제목 밑에 `- 결정: …` 을 적는다 — 다음 회차의 세션이 읽는다
4. **답한 항목은 `archive/decisions-done.md` 로 내린다** — 여기는 **아직 답하지 않은 것만** 둔다.
   안 치우면 답한 것도 대시보드의 「결정 대기」에 계속 남아 몇 건인지 알 수 없다 (2026-09-24)

넘어온 묶음의 **통과한 단계는 브랜치(`g/<ID>`)에 그대로 남아 있다.** 이어서 하면 된다.

---

## G-145 — 건축을 다시 짠다 — 도면 · 등급 · 이어 붙이는 개수
- 넘긴 때: 2026-09-24 14:58:04
- 막힌 단계: 1. 장 수를 버리고 이어 붙인 개수로 잰다
- 통과한 단계: 없음
- 왜 막혔나: test_from_empty_hands.gd 의 건축 부분(G-903.4 를 하다가 커밋했고, G-145 뒤로 미뤄 둔 것)이 HEAD 에서도 떨어진다. 그래서 G-145 의 어느 단계를 해도 기계 검사가 떨어진다. 과녁이 길을 막는 것은 테스트만 고치면 되지만, 캐릭터가 0.2m 토대 위로 걸어 오르지 못하는 것은 게임 규칙 문제다. 어느 쪽으로 할지 정해 달라: (가) 턱을 오르는 높이를 정해 준다(예: 0.3m까지는 걸어서 오른다) (나) 토대를 땅 높이에 맞춘다 (다) G-903.4 를 다시 할 때까지 그 부분을 테스트에서 빼 둔다
- **사람이 답할 것**: 

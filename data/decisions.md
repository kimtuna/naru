# 결정 대기

루프가 스스로 풀지 못한 것을 여기로 넘긴다. **사람은 이 파일과 `list.md` 만 고친다.**

## 어떻게 답하나
1. 아래 항목의 「사람이 답할 것」에 답을 적는다
2. `list.md` 에서 그 묶음의 `[>]` 를 `[ ]` 로 되돌린다
3. 묶음 제목 밑에 `- 결정: …` 을 적는다 — 다음 회차의 세션이 읽는다

넘어온 묶음의 **통과한 단계는 브랜치(`g/<ID>`)에 그대로 남아 있다.** 이어서 하면 된다.

---

## 물음 — 전체 테스트 501초가 하네스 제한 600초에 닿는다
- 물은 때: 2026-09-22 16:59:46
- 어디서: G-124 단계 3
- 무엇: 전체 테스트 501초가 하네스 제한 600초에 닿는다 — harness/bin/godot:14 의 LIMIT 기본값이 600초인데 이번 전체 실행이 501.8초였다 (.loop/out/test.log 의 Run Summary). 묶음이 하나만 더 무거워지면 합치기 전 전체 실행이 통째로 끊긴다. 하네스는 세션이 못 고친다 (LOOP.md 1절) — 제한을 올릴지, 전체 실행을 폴더별로 쪼갤지, 느린 테스트의 실제 기다림을 줄일지 사람이 골라야 한다. 같은 파일 12번 줄의 실측 주석도 낡았다: 「전체 테스트 55개 94초」인데 지금은 87개 파일 781개 501초다
- **사람이 답할 것**: 

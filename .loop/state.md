# 루프 상태

과제: `BACKLOG.md` 순서대로. 지금은 P0.

## 회차

### 회차 32 — 사람이 차지한 칸 · `Claim` · REGROW ⑤⑥ · 등록 빠뜨리기 대조군
- **목록을 월드 밖에 냈다**: `Claim` 이 **이름 붙은 출처**를 모으고 월드는 여전히
  Callable 하나만 안다(`world.occupied = claim.covers`). `WorldState` 안에 if 로 쌓으면
  월드가 설치물·밭·길을 다 알게 되고, `_body_covers` 에 쌓으면 어느 회차가 조용히
  한 줄을 빼먹는다 — 새 설치물은 **남의 함수를 고치는 게 아니라 한 줄을 더한다**
- **계약은 선언과 배선을 가른 자리에 있다**: `Claim.KINDS` 에 적고 `add()` 로 꽂는다.
  안 맞는 것은 `missing()` 이 세고 **REGROW 가 진짜 씬에서** 읽는다(⑤). 월드가 **목록을
  거치는지**도 같이 본다 — 몸 하나를 바로 꽂으면 `missing()` 은 비어 있는데 설치물은
  영영 안 물어진다. 이 둘이 이 회차가 진짜로 만든 것이다
- **설치물이 없어도 다 잴 수 있다**(⑥): 살아 있는 `claim` 에 **가짜 설치물** 한 칸을
  꽂는다. **임자의 이름까지 본다** — 「안 자랐다」만 보면 몸이 막은 것과 구별이 안 돼서
  새 출처를 안 물어도 초록이다. 단위 검사는 **옆 칸이 자라는 것**을 같이 센다:
  설치물 하나가 온 섬의 재생을 멈춰도 「안 자란다」는 초록이다
- **가짜 설치물의 이름은 `KINDS` 밖에 둔다** — 검사가 꽂는 것을 선언에 섞으면
  아무도 안 꽂아도 `missing()` 이 비어서 ⑤ 가 통째로 무너진다
- **대조군이 두 번 헛돌았다**(GOTCHAS 대조군 절): 대조군을 써 넣는 python 이 heredoc 안
  python 의 `\n` 을 먼저 먹었다 — 안쪽이 SyntaxError 로 죽고 **코드는 멀쩡히 남는다.**
  탭은 살아남아 넷 중 둘만 깨졌다. 「게이트가 약하다」가 아니라 **아무것도 안 깨뜨렸다**
- 같이 고친 둘: `test_claim` 이 「빠진 것 0 개」로 세고 있었다(제작대가 선언되는 날
  엉뚱한 검사가 빨개진다) · REGROW 가 배선 때문에 죽어도 「설 자리를 못 잡았다」를 찍었다
- 실측: `REGROW ok (하루 1200 s · 나무 1.0일 · 비킨 뒤 1.0 s · 남은 벤 칸 0 ·
  차지 출처 2 종)` · `설치물 하루+1초 뒤 그대로 (임자 가짜 설치물) · 허문 뒤 나무` ·
  `TESTS 164 passed, 0 failed`(154 → 164) · `run-contract.sh` 30.7초 · `ALL GREEN` 9/9 ·
  `REDTEAM 11 잡음, 0 놓침`(`--only 회차 32` · 4분 15초) — **그중 둘은 단위 검사를
  전부 초록으로 남긴다**
- 남긴 것: **반경**(설치물이 화면에 서야 눈으로 정한다) · 다음은 **전체 화면 띠 최소화** ·
  **[ASK] 밤이 얼마나 어두워야 하나**

### 회차 33 — 밤을 0.290 → 0.180 · 8비트 바닥
- **값이 아니라 비의 문제였다**: 채널 비를 두고 통째로 0.6207 배. 부등식을 뒤집는 것은
  **b/r 하나**고 밝기는 양변을 똑같이 곱해 **문턱 1.0933 이 안 움직였다** (여유만 0.00698)
- **바닥은 8비트가 정한다**(새로 안 것): 그 여유는 float 인데 나가는 것은 반올림한 픽셀이다.
  구운 땅의 `r−b` 최소가 **1/255**, 0.072 에서 **0** — 그때도 float 부등식은 참이라 기존
  검사는 초록이다. 대조군이 **기존 문 넷의 틈**(밝기 0.110 · b/r 1.0893)을 찾아 그 자리를 쨌다
- 처음 쓴 대조군 0.072 는 `night > 0.1` 도 같이 깨서 **증거가 못 됐다.** 회차 31 의 파란
  밤빛 대조군은 옛 리터럴을 집고 있었다 — **값을 바꾼 회차는 자기를 겨누는 대조군도 옮긴다**
- 실측: `DRAW [밤] 2c2e2f (밝기 0.180) · 불일치 0 · 최대 색차 0.9/255 · 채운 5 → 5` · 색 수
  200 → **117** · `TESTS 165 passed, 0 failed` · `REDTEAM 14 잡음, 0 놓침`
- 남긴 것: **램프를 켜면 밤빛을 다시 맞춘다**(더 내리면 8비트 바닥이 먼저 걸린다) · 다음은
  **전체 화면 띠 최소화**. state.md 는 한 회차에 **11줄**이다 — doclen 이 roll 보다 먼저 돈다
### 회차 34 — 창을 게임 크기에 · 띠 0x0 (일지 번호 33)
- **띠는 창이 게임보다 커서 생긴다 — 창을 게임에 맞춘다.** `Display.fit_window()` 가 최대
  정수 배율 N 으로 창을 `논리 × N` 에 잡고 가운데 놓는다. 전체 화면을 그만뒀다.
  **「셋 중 둘만」이 틀렸다**(ⓐ띠 없음 ⓑ정수 배율 ⓒ같은 범위) — **고정인 것은 전체
  화면일 때뿐**이었다. 그림은 그대로다(3배·2880x1620·60.00 x 33.75). 더 준 건 테두리뿐
- **여태 게이트에 안 걸린 이유**: 「띠 = 0」 하나로는 **아무것도 안 잡는다** — override
  1920x1080 이 정확히 2배라 배선을 빼먹어도 0 이다. VIEW 가 **띠 0 · 배율 최대 · 자리
  가운데** 셋을 묻고 띠는 **창에서** 뺀다
- **대조군 ⑤ 를 두 번 놓쳤다**(GOTCHAS): ① 크기만 재고 자리를 안 봤다 — 그건 **자리만**
  움직인다(340 → 307) ② 기대값을 `Display.avail_rect()` 로 냈다 — **대조군이 고치는 그
  함수**라 「틀린 값끼리 같다」. **재는 쪽은 재이는 쪽의 말을 빌리면 안 된다**
- 실측: `창 2880x1620 @ 288,340 · 배율 3.00x · 띠 0x0 · 화면 남음 576x548` · `TESTS 175
  passed, 0 failed`(173 →) · `ALL GREEN` 9/9 · `REDTEAM 12 잡음, 0 놓침`(`--only` 이 묶음)
- 남긴 것: 다음 항목(**띠 0 을 게이트로**)은 대부분 했다 — **전체 판** 확인만 남았다 ·
  **[ASK] 밤이 얼마나 어두워야 하나**

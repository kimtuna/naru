# 루프 상태

과제: `BACKLOG.md` 순서대로. 지금은 P0.

## 회차

### 회차 42 — 가방 화면: `E` 로 열고 닫는다 · 18칸 + 핫바 9칸 (일지 번호 40)
- **핫바 바로 위에 2 × 9 로 뜬다.** 18 = 2 × 9 라 **열이 핫바와 정확히 같은 x** 에 선다 —
  다음 항목(칸 사이 옮기기)이 위아래로 곧장 오가고, 핫바를 안 가려서 「무엇을 들었나」를
  보면서 정리한다. 창 `(322,414) 316 × 78 px`. 자리는 순수 함수(`panel_rect`·`slot_rect`)라
  **그리는 쪽과 게이트가 같은 수**를 쓴다 (회차 20 이 핫바에서 배운 그대로)
- **칸 하나의 그림을 한 벌로 모았다** — `HotbarView.draw_slot(on, r, id, amount, edge)`.
  창마다 따로 그리면 같은 물건이 다르게 보이고 게이트의 탐침 자리가 둘로 갈라진다
- **BAG 게이트가 가방이 가린 자리를 대신 판정한다.** `E` 를 눌러 **세 번 굽는다**:
  ① 닫힌 채 54점이 월드 색인가 ② 연 채 18칸 × 세 점이 제 색이고 **찬 칸과 빈 칸이
  달라 보이나**(칸 한가운데를 읽는다) ③ 다시 닫고 **24,648 픽셀이 하나까지** 같은가.
  **DRAW 는 이 자리를 안 건너뛴다** — 창은 닫힌 것이 기본이라, 누가 기본으로 열어 두면
  DRAW 가 「화면이 월드와 다르다」로 빨개진다
- **대조군이 내 설명을 깨뜨렸다.** 굽은 PNG 가 닫은 것과 같았던 것을 「숨은 `CanvasItem`
  은 다시 그리기가 버려진다」로 읽었는데, `toggle` 에서 `queue_redraw` 를 빼도 `ALL GREEN`
  이다. 진짜 원인은 **씬의 `_ready` 가 첫 프레임에 돌면서 도로 닫은 것**이었다
- **대조군 여섯** (`--only 회차 40 가방 화면`): ① E 를 안 읽는다 ② 창을 안 그린다
  ③ 창 바탕을 안 칠한다 ④ 여는 것만 된다 ⑤ 아이템 네모를 안 그린다 ⑥ 9칸으로 줄인다.
  **①~⑤ 는 단위 검사 194개를 전부 초록으로 남긴다**
- **옆 세션이 또 워킹트리를 되돌렸다** (회차 40·41 에 이어 세 번째) — 이번엔 커밋하기
  직전에 통째로 날아갔다. `/tmp/naru40/replay*.py` 로 되살렸고, 그 뒤로 **게이트 전에 먼저
  커밋**했다. 대조군은 `git worktree add --detach` 로 떼서 돌렸다
- 실측: `TESTS 194 passed, 0 failed`(전 183) · `ALL GREEN` 9/9 ·
  `BAG [닫힌 채] 탐침 54 · 어긋남 0` · `[연 채] 찬 칸 2 · 빈 칸 16 · 달라진 점 54/54` ·
  `[다시 닫고] 픽셀 24,648 · 최대 색차 0.0/255`
- 남긴 것: **가방에 물건이 들어오는 길이 아직 없다** — 바닥의 것을 줍는 항목(P2)이 낸다.
  다음 셋이 이 화면 위에 얹힌다: 열린 동안의 입력 갈래 · 칸 사이 옮기기 · 반 집기.
  `last-full-redteam` 은 25 다

### 회차 43 — 가방이 열려 있는 동안의 입력: 걷기는 살고 좌클릭·숫자키는 죽는다 (일지 번호 42)
- **갈래는 표 하나다** (`scripts/input_route.gd`): 이동 · 좌클릭 · 숫자키 · 가방키 넷에
  「창이 열린 동안 사나」를 적었다. `main.gd` 의 `_poll_*` 세 곳은 **창이 열렸나만**
  묻는다(`_ui_open`) — 조건을 폴링하는 쪽마다 적으면 창이 늘 때(상자 · 제작대) 어느
  회차가 한 곳을 빼먹고, 그 빼먹음은 「정리하다 손이 바뀐다」로만 보인다
- **가방 키도 표에 묻는다.** 처음엔 「늘 참이라 물을 필요 없다」고 뒀는데, 그러면 그 줄은
  아무도 안 읽는 죽은 줄이고 대조군이 아무것도 안 잡는다 (회차 42 가 배운 그 자리)
- **눌렀다 뗐다만 재면 안 보이는 구멍이 있었다**: 죽어 있는 동안 직전 프레임을 안 적으면
  연 채 누른 숫자키가 **창을 닫는 그 프레임에** 손을 옮긴다 — 사람은 아무것도 안 누른
  프레임이다. 그래서 FOCUS 에 **키를 쥔 채로 닫는** 구간을 따로 뒀다
- **FOCUS 게이트**(`measure_focus.gd` · 헤드리스 여덟째 구간) 여섯 구간: 열린 채
  좌클릭 · 숫자키 · WASD · 쥔 키로 닫기 · **닫은 뒤 둘(게이트 안의 대조군)**.
  대조군이 없으면 「안 휘둘렀다」와 「클릭이 애초에 안 갔다」가 구별되지 않는다 (회차 29)
- **허용치가 하나도 없다** — 입력이 막히면 정확히 0.0 px, 살아 있으면 0.30초에 72.00 px
  라서 잡음이 낄 자리가 없다. 그래서 `Tol.obs` 로 흘릴 것도, 표에 적을 줄도 없다
- **대조군 일곱** (`--only 회차 43 가방이 열려 있는 동안의 입력`): ①② 좌클릭·숫자키가
  갈래를 안 묻는다 ③④ `_ui_open` 이 늘 거짓 / 늘 참 ⑤ 죽은 동안의 직전 프레임을 안
  적는다 ⑥⑦ 표에서 이동 / 가방 키를 죽인다. **①③⑤ 는 단위 검사 202개를 전부 초록으로
  남긴다 — 세 개 다 직접 재 봤다** (회차 42 가 안 잡는 대조군에 속은 뒤로)
- 실측: `TESTS 202 passed, 0 failed`(전 194) · `ALL GREEN` 9/9 · `REDTEAM 14 잡음, 0 놓침` ·
  열린 채 좌클릭 60프레임 중 휘두른 프레임 **0** · 닫은 뒤 같은 0.41초에 **59** ·
  열린 채 걷기 **72.00 px (4.50칸 · 236.8 px/s)** · 쥔 키 5 로 닫아도 손은 1번 칸
- 남긴 것: **좌클릭을 쥔 채로 닫으면 곧바로 휘두른다** — 코어 키퍼와 같아서 뒀고
  `[ASK]` 로 넘겼다. `last-full-redteam` 은 25 다

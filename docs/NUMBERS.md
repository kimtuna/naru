# NUMBERS — 실측으로 얻은 값만

> **이 파일은 혼자 선다.** 다른 문서를 안 봐도 읽힌다.
>
> **값보다 「조건」이 중요하다.** 아래 값은 전부 어떤 기계 · 어떤 엔진 버전 위에서
> 잰 것이다. 조건이 다르면 그대로 못 쓰고, **비율로 환산해도 안 되는 것이 있다.**
>
> 값을 고치려면 **새로 재고 조건을 같이 적는다.** 「passed」는 증거가 아니다.

---

## 절 목차 — **통째로 읽지 마라. 필요한 절만 grep 한다**

| 절 | 무엇 |
|---|---|
| 0 | 환경 — 엔진 버전 · 경로 · 렌더러 |
| 1 | 화면 — 논리 해상도 · 창 · 배율 · 보이는 칸 |
| 2 | 격리 — macOS 는 XDG 를 무시한다 |
| 3 | 검사 — 계약 · 대조군 결과 |
| 3b | 화면 캡처 — 헤드리스로는 안 된다 |
| 4 | (함정은 `GOTCHAS.md` 로 옮겼다) |
| 5 | 이동 — 속도 실측 |
| 6 | 월드 — 시드 생성 · 섬 모양 · 프로세스 간 재현성 |
| 7 | 충돌 — 몸 크기 · 벽에 붙는 자리 · 미끄러짐 |
| 8 | 카메라 — 따라가기 · 줌 없음 · 화면 좌표 |
| 9 | 그리기 — 보이는 칸만 · 타일 색 · 색 캐시 |

---

## 0. 환경 (2026-09-13 실측)

| | |
|---|---|
| 엔진 | **Godot 4.7.2.stable.official.ed1daf0bf** |
| 경로 | `/Applications/Godot.app/Contents/MacOS/Godot` — **PATH 에 없다.** 전체 경로로 부른다 |
| 기계 | macOS · Apple M5 Pro |
| 렌더러 | `OpenGL API 4.1 Metal - 90.5 - Compatibility` (`gl_compatibility`) |

---

## 1. 화면 (2026-09-13 실측)

**조건: `stretch/mode=viewport` + `scale_mode=integer` + `aspect=keep`, 창 override 1920×1080**

실제로 창을 띄워서 잰 값이다 (`scripts/main.gd` 가 찍는다):

```
VIEWPORT 960 x 540
WINDOW   1920 x 1080
SCALE    2.00 x
```

| 값 | 무엇 | 왜 그 값인가 |
|---|---|---|
| **960 × 540** | 논리 해상도 (= 보이는 월드 범위) | **1080p 정수 2배 · 4K 정수 4배.** 소수 배율은 도트 하나를 화면에서 어떤 건 1px, 어떤 건 2px 로 만들어 걸을 때 얼룩이 흐른다 |
| **1920 × 1080** | 창 크기 | 논리 화면을 정확히 2배로 늘려 붙인다 |
| **2.00 ×** | 실측 배율 | 정수다 |
| **20 × 11.25 칸** | 보이는 타일 수 | 타일 48px 기준. `test_visible_tiles_at_48px` 가 강제한다 |

**대가**: UI 도 960×540 좌표계에서 만들어야 하고, 폰트가 통째로 2배로 늘어나므로
**비트맵 픽셀 폰트가 필요하다** — 기본 폰트는 늘리면 뭉갠다.

### 실측 게이트 `tools/tests/measure_view.gd` (2026-09-14 실측)

위 표는 `test_project_settings.gd` 가 **project.godot 의 글자**를 읽어 강제한다.
글자가 맞아도 실행 중에 카메라 줌이나 창 크기를 만지면 눈에 보이는 칸은 달라진다 —
그래서 **실제 창을 띄워** 다시 잰다 (`check.sh tests` 가 `--headless` 없이 부른다).

```
VIEW 논리 960x540 · 창 1920x1080 · 배율 2.00x · 카메라 1.00x · 타일 48px · 보이는 칸 20.00 x 11.25
VIEW ok (드라이버 macOS · 프레임 5)
```

- 배율은 나눗셈이 아니라 **`root.get_final_transform()`** 에서 잰다.
- 보이는 칸은 **`get_canvas_transform()`** 으로 월드 좌표로 환산해서 잰다 —
  카메라 줌이 곧 시야 이득이라 여기를 막아야 한다 (BACKLOG P1 「줌 없음」).

**대조군 2종** (`redteam.sh` 에 박아 뒀다):

| 일부러 깨뜨린 것 | 단위 검사 | 화면 실측 |
|---|---|---|
| 카메라 `zoom` 을 (2,2) 로 | (2026-09-13: `18 passed`) | **잰 값 보이는 칸 10.00 x 5.62** ✅ |
| 실행 중 `window_set_size(1600, 900)` | `18 passed, 0 failed` | **잰 값 창 1600x900 · 배율 1.00x** ✅ |

두 번째가 정수 배율의 성질을 그대로 보여준다: 1600/960 = 1.66 이 아니라 **1** 로 떨어진다.

**첫 줄은 바퀴 8 에서 겨눌 곳이 바뀌었다.** 그때는 `main.tscn` 에 카메라를 하나 덧붙였는데,
진짜 카메라가 생긴 뒤로는 **먼저 트리에 들어온 플레이어의 카메라가 화면을 잡는다(선착순)** —
덧붙인 카메라는 아무 일도 안 한다. **가짜 대조군이 될 뻔한 자리다.** 지금은 `player.tscn` 의
그 카메라의 줌을 직접 건다. 대신 단위 검사(`test_camera_zoom_is_one`)도 같이 잡으므로,
「단위는 초록인데 실측만 잡는」 쪽은 8절의 **실행 중 줌**이 맡는다.

---

## 2. 격리 — **macOS 는 XDG 를 무시한다** (2026-09-13 실측)

**이걸 안 재고 넘어갈 뻔했다.** `XDG_DATA_HOME` 등을 설정해두고 「격리했다」고 믿는 것이
정확히 가짜 게이트다.

| 방법 | `user://` 가 실제로 간 곳 | 홈의 Godot 디렉터리 |
|---|---|---|
| `XDG_*` 만 설정 | `~/Library/Application Support/Godot/app_userdata/Naru` | **mtime 이 실행 시각으로 갱신됨** ❌ |
| **`HOME` 을 돌린다** | `.godot-home/Library/Application Support/Godot/app_userdata/Naru` | **mtime 안 바뀜** ✅ |

- 그래서 `tools/loop/godot.sh` 는 **`HOME` 자체를 프로젝트 안으로 돌린다.**
  `XDG_*` 도 같이 두는 것은 리눅스에서 도는 경우를 위해서다.
- **격리는 설정이 아니라 「입구」로 지켜진다.** `test_isolation.gd` 가
  `OS.get_user_data_dir()` 이 프로젝트 안인지 재므로, **`godot.sh` 를 우회한 실행은
  그 자리에서 빨개진다.** 실측:

  ```
  FAIL test_isolation.gd :: test_user_data_is_project_local
       — 잰 값: /Users/tuna/Library/Application Support/Godot/app_userdata/Naru
  ```

---

## 3. 검사 (P0-1 시점)

`bash tools/loop/check.sh all` → `IMPORT ok` · `PARSE 5개 스크립트, 실패 0` ·
`TESTS 8 passed, 0 failed`

### 대조군 — 게이트가 진짜로 잡는지 잰 값

**검사를 만든 바퀴에 이걸 안 돌리면, 아무것도 안 잡는 게이트가 쌓인다.**

| # | 일부러 깨뜨린 것 | 결과 |
|---|---|---|
| 1 | 문법이 깨진 `.gd` 를 하나 넣음 | `PARSE 5개, 실패 1` ✅ |
| 2 | 논리 가로 960 → 1280 | `TESTS 4 passed, 2 failed` — 잰 값 26.67칸 ✅ |
| 3 | 텍스처 필터 nearest → linear | `TESTS 5 passed, 1 failed` ✅ |
| 4 | stretch integer → fractional | `TESTS 5 passed, 1 failed` ✅ |
| 5 | `godot.sh` 를 우회해서 직접 실행 | `TESTS 7 passed, 1 failed` ✅ |

전부 원복하면 다시 `TESTS 8 passed, 0 failed`.

### 계약 채점자의 대조군 (2026-09-13)

**채점자가 진짜로 잡는지 재야 한다** — 안 재면 「전부 초록인데 게임은 안 도는」 상태로 며칠을 간다.

| 상황 | 결과 |
|---|---|
| 정상 | `exit 0` · `all_green true` |
| 기준 하나 실패 | `exit 1` · **증거에 잰 값**(`잰 값 1280 · 기대 960`) |
| 무장 뒤 계약 변조 | **`exit 77`** · 무장/현재 해시를 둘 다 기록 |
| 공허한 계약 (전부 주석) | **`exit 78`** — 무장 중에는 77 이 먼저 걸린다(변조가 우선). 무장을 풀고 재서 확인했다 |
| 계약 파일 없음 | **`exit 78`** |

- **BSD `tr` 은 `\x1f` 를 못 읽는다.** 증거 줄을 이어붙이는 구분자가 문자 `x` 가 되어
  results.json 의 증거가 한 줄로 뭉쳐 있었다. 8진수 `\037` 로 고쳤다.

## 3b. 화면 캡처 (2026-09-13 실측)

**`screencapture` 는 못 쓴다** — macOS 화면 기록 권한에 막힌다
(`could not create image from display`). 무인 루프가 권한 대화상자를 넘길 방법이 없다.

**Godot 자신의 프레임버퍼를 읽는다** (`tools/qa/shot.gd`). 권한이 필요 없고,
화면에 보이는 것이 아니라 **게임이 그린 것**을 읽으므로 다른 창이 겹쳐도 상관없다.

| 방식 | 결과 |
|---|---|
| `--headless` + 뷰포트 텍스처 | **안 된다** — 렌더러가 더미라 텍스처가 빈다 (출력 없음) |
| **창을 띄우고 뷰포트 텍스처** | **된다** — `SHOT 960x540 색 3개 가장 넓은 한 색 50.0%` |

- 그래서 `tools/loop/shot.sh` 는 `--headless` 를 **쓰지 않는다.** 창이 잠깐 떴다 사라진다.
- **「저장됨」은 증거가 아니다** — 크기 · 색 수 · 가장 넓은 한 색의 비율을 같이 찍는다.
  `--max-flat <퍼센트>` 로 「거의 단색인 화면」을 빨갛게 만들 수 있다.
- 대조군: 상한 40% → 빈 씬(50%)이 잡힘 / 없는 씬 → `SHOT ERROR` / 헤드리스 → 출력 없음.

---

## 4. 함정은 `GOTCHAS.md` 로 옮겼다

엔진·셸의 함정은 **계속 쌓이는 파일**이라 여기 두면 이 문서가 무한히 는다.
**에러가 났을 때만** `GOTCHAS.md` 를 grep 한다.

---

## 6. 월드 (2026-09-14 실측)

**조건: `scripts/world_gen.gd` · Godot 4.7.2 · macOS M5 Pro · 헤드리스 · 단일 스레드**

| 값 | 무엇 |
|---|---|
| **256 × 256** | 월드 한 변 = 65,536 타일 (48px 기준 12,288 px 사방) |
| **2종** | 물(0) · 땅(1). 나무·돌은 P2 에서 이 위에 얹는다 |
| **190 ms** | 한 장 생성 (3장 569 ms). 로딩 화면 없이 넘어갈 수 있는 값이다 |
| **28.00 % ~ 40.97 %** | 땅 비율. **씨앗 12개 실측**(0·1·2·3·7·42·-7·±20260914·999983·2147483647·123456789) |

### 섬 모양은 운이 아니라 **부등식**이다

| 보장 | 왜 참인가 |
|---|---|
| 테두리는 **항상 물** | 감쇠 `FALLOFF_GAIN 1.25` > 잡음 최대 `1.0` → 높이 ≤ -0.25 < 해수면 0.30 |
| 한가운데는 **항상 땅** | 잡음 바닥 `NOISE_FLOOR 0.35` > 해수면 `SEA_LEVEL 0.30`, 중심 감쇠 ≈ 0 |

씨앗을 몇 개 뽑아 봐서 「되더라」가 아니라 상수끼리의 부등식이다.
**상수를 만지면 이 두 줄부터 다시 계산해야 한다** — 검사(`test_border_is_always_water`,
`test_spawn_is_always_land`)가 여러 씨앗으로 지키지만, 검사는 표본이고 위 두 줄은 증명이다.

### 잡음 상수 — 눈으로 고른 값

| 상수 | 값 | 무엇이 달라지나 |
|---|---|---|
| `BASE_CELLS` | 3.5 | 잡음 한 칸 ≈ 73타일. 큰 만(灣)의 크기 |
| `CONTRAST` | 2.5 | **1.0 이면 해안선이 동그란 덩어리가 된다.** 실제로 그렇게 나와서 올렸다 |
| `OCTAVES` | 4 | 해안선의 잔 들쭉날쭉 |
| `FALLOFF_POW` | 3.0 | 클수록 섬이 넓고 해안이 가파르다 |

**이 넷은 재서 얻은 값이 아니라 PNG 를 보고 고른 값이다** (`tools/qa/world_png.gd`).
땅 비율·체크섬은 「섬처럼 생겼나」를 말해 주지 않는다 — 같은 35% 가 도넛일 수도 점 100개일 수도 있다.

### 실측 게이트 `tools/tests/measure_world.gd` (2026-09-14 실측)

`check.sh tests` 가 `FACE` 뒤에 **엔진을 두 번 띄워** 부르고, `WORLD` 줄을 글자까지 비교한다.
헤드리스로 된다 — 순수 계산이라 창이 필요 없다.

```
WORLD 256x256 · 씨앗 1 · 땅 38.07% · 체크섬 0x22a64c62
WORLD 256x256 · 씨앗 42 · 땅 39.01% · 체크섬 0x4d412265
WORLD 256x256 · 씨앗 -20260914 · 땅 28.31% · 체크섬 0xb5650262
WORLDGEN 3장 · 569 ms
WORLD 두 프로세스 체크섬 일치
```

**시간은 `WORLD` 줄에 넣지 않는다** — 실행마다 달라서 비교가 늘 빨개진다. 그래서 `WORLDGEN` 은 따로다.

### 대조군 — P1-4 로 새로 넣은 3종 (2026-09-14 실측)

| 일부러 깨뜨린 것 | 단위 검사 35개 | 결과 |
|---|---|---|
| 씨앗에 `Time.get_unix_time_from_system()` 을 섞음 | **전부 초록** | **`WORLD` 만 잡았다** — 프로세스마다 체크섬이 다름 |
| 가장자리 감쇠를 뺌 (`_falloff` → 0.0) | 빨강 | `test_border_is_always_water` |
| `SIZE` 256 → 128 | 빨강 | `test_size_and_two_kinds` |

**첫 줄이 이 게이트의 존재 이유다.** 정적 변수는 프로세스마다 한 번만 초기화되므로
**한 프로세스 안에서는 늘 같은 월드**다 — 「같은 씨앗 = 같은 월드」를 한 프로세스 안에서만
재는 검사는 전부 초록으로 남는다. 사람 눈에는 「어제 만든 섬이 오늘 다르다」로만 보인다.
바퀴 3(속도)·바퀴 4(화면)·바퀴 5(방향)와 **같은 모양의 구멍**이고, 넷 다 실측 게이트로 막았다.

전체: `REDTEAM 18 잡음, 1 놓침` (놓친 하나는 3절의 「검사를 지우면 바닥이 잡는다」 —
검사가 35개인데 `mintests` 바닥이 26 이라 헐겁다. 바닥은 무장된 `criteria.tsv` 안에 있다)

---

## 7. 충돌 (2026-09-14 실측)

**조건: `scripts/world_collide.gd` · Godot 4.7.2 · macOS M5 Pro · 헤드리스 ·
물리 60 Hz · 씨앗 20260914 · 메인 씬을 통째로 띄워 잼**

| 값 | 무엇 |
|---|---|
| **22.0 px** | 몸 반폭(`HALF`). 몸 44×44 가 48 타일 안에서 **양옆 2px 씩 남는다** |
| **0.01 px** | 벽에 붙일 때 남기는 틈(`EPS`). 0 이면 붙은 칸이 겹친 칸으로 읽혀 다음 프레임이 흔들린다 |
| **169.71 px/s** | 해안을 탈 때의 세로 속력 = 240 / √2. **벽에 붙어도 안 줄인다** |
| **3.302 µs** | 「이 칸이 막나」 한 번 (10만 회 330.2 ms · 막힘 67.7%). 한 틱에 네 번쯤 묻는다 |

**몸이 타일보다 좁아야 하는 이유**: `HALF` 가 타일 반(24)과 같으면 폭 1칸(48px) 통로에서
부동소수 한 톨에 걸려 낀다. `22 < 24` 는 검사(`test_body_is_narrower_than_a_tile`)가 지킨다.

### 미끄러짐은 **축을 따로 푸는 것**에서 나온다

x 를 풀고 y 를 푼다 — 막힌 축만 죽고 나머지는 그대로 간다.
「벽에 닿았으니 통째로 정지」가 아니고, 남은 몫을 다시 정규화하지도 않는다.
**물리 엔진을 안 쓴다**: 65536칸에 정적 몸을 세울 수 없고, 창 주변만 세우면 결과가
「지금 무엇이 세워져 있나」에 달린다. 격자에 대고 풀면 **같은 입력이 언제나 같은 자리**다
(GDD D-1 서버 재현성 · C-4 원정 보고 검증).

### 실측 게이트 `tools/tests/measure_collide.gd` (2026-09-14 실측)

`check.sh tests` 가 `WORLD` 뒤에 부른다. **메인 씬을 통째로 띄운다** — 플레이어 씬만
띄우면 `main.gd` 의 배선이 안 잡힌다. 씨앗에서 **곧은 남북 해안**(땅 4칸 × 7줄 + 옆은 바다)을
스폰에서 가까운 순으로 찾아, 활주로 3칸에서 걸어 부딪힌다.

```
COLLIDE 해안 월드칸 (229, 122) · 스폰에서 101.2칸 · 활주로 3칸 · 바다 면 11040.00 px
COLLIDE 바다로 직진       x 11017.99 (바다 면 11040.00) · 세로    0.00 px/s · 1.0035 s 동안 (145.9902, 0.0)  ok
COLLIDE 해안 미끄럼       x 11017.99 (바다 면 11040.00) · 세로  169.70 px/s · 1.2001 s 동안 (145.9902, 203.6602)  ok
COLLIDE 질의 100000회 330.2 ms (1회 3.302 µs · 막힘 67.7%)
COLLIDE ok (구간 2 · 몸 반폭 22 · 틈 0.01 · 물리 60 Hz)
```

**2026-09-14 카메라(8절)와 함께 다시 쟀다.** 값이 5376 → 11040 으로 옮겨간 것은 물리가
아니라 **좌표계**다: 이제 화면 칸 = 월드 칸이라 해안 칸 230 이 그대로 230 × 48 = 11040 px 다.
멈춘 자리 **11017.99 = 11040 - 22 - 0.01** 이다. 세 가지를 같이 본다:
① 바다 면 앞에 서는가 ② **몸이 바다에 걸치지 않는가**(좌표만 맞고 반 칸 잠기는 경우가 있다)
③ 옆으로 가려던 몫이 살아 있는가(169.71 ±5).

### 화면 칸 ≠ 월드 칸 — **사라진 값** (2026-09-14 · 바퀴 8)

바퀴 7 에는 `main.gd` 에 `tile_offset = (118, 123)` 이 있었다. 카메라가 없어서 플레이어가
화면 안(504, 264 = 화면 칸 10,5)에 있어야 했는데 섬의 스폰은 월드 한가운데(128,128)였기
때문이다. **카메라가 오면서 그 값은 0 이 아니라 아예 없어졌다** (8절):
플레이어는 스폰 칸의 한가운데 (6168, 6168) 에 그냥 서 있다. `solid_from_seed` 의 이동량
인자와 `main.gd` 의 `screen_of()` 도 같이 지웠다 — **화면 칸 = 월드 칸**이다.

### 대조군 — P1-5 로 새로 넣은 3종 (2026-09-14 실측)

| 일부러 깨뜨린 것 | 단위 검사 53개 | 결과 |
|---|---|---|
| 노드가 충돌을 안 묻는다 (`position += velocity * delta`) | **전부 초록** | **`COLLIDE` 만 잡았다** — 바다 위를 걸어간다 |
| `main.gd` 가 월드를 안 꽂는다 (`player.solid` 가 빈 Callable) | **전부 초록** | **`COLLIDE` 만 잡았다** — 막는 칸이 하나도 없다 |
| 벽에 닿으면 통째로 멈춘다 (축을 따로 안 푼다) | 빨강 | `test_diagonal_into_the_coast_slides` + `COLLIDE` |

앞의 두 줄이 **또 같은 모양의 구멍**이다 (바퀴 3 속도 · 4 화면 · 5 방향 · 6 월드):
순수 계산은 멀쩡한데 **노드·씬이 그걸 안 쓴다.** 다섯 번 다 실측 게이트로만 잡혔다.

전체: `REDTEAM 20 잡음, 2 놓침`. 놓친 둘은 게이트가 아니라 **환경과 바닥**이다 —
① 3절의 「검사를 지우면 바닥이 잡는다」(검사 53 · `mintests` 바닥 35. 무장된 `criteria.tsv` 안에 있다)
② 마지막 「원복하면 다시 초록이다」가 `FACE` 로 한 번 튀었다 — **사람이 마우스를 건드리면
빨개지는 게이트**다(GOTCHAS). 같은 트리에서 곧바로 다시 돌린 계약은 `ALL GREEN` 이다.

---

## 8. 카메라 (2026-09-14 실측)

**조건: `scenes/player.tscn` 의 `Camera2D` · Godot 4.7.2 · macOS M5 Pro · 헤드리스 ·
물리 60 Hz · 씨앗 20260914 · 메인 씬을 통째로 띄워 네 방향으로 걸으며 잼**

| 값 | 무엇 |
|---|---|
| **0.0000 px** | 걷는 내내 플레이어가 화면 중심에서 벗어난 **최대** 거리 (표본 294) |
| **1.00 x** | 캔버스 변환의 줌. **보이는 칸 20.00 x 11.25 — 1절과 같은 값이다** |
| **(6168, 6168)** | 스폰 자리 = 칸 (128,128) 의 한가운데. 화면으로는 **(480, 270)** = 정확히 한가운데 |
| **[P: (5688, 5898), S: (960, 540)]** | 스폰에서 보이는 월드 범위(px). 논리 화면과 크기가 같다 = 줌이 없다 |

### 카메라는 **코드가 아니라 부모-자식**이다

`Camera2D` 를 플레이어 씬 안에 둔다. 매 프레임 `camera.position = player.position` 을
쓰지 않는다 — 그건 물리(60Hz)와 그리기 사이에 **한 틱 뒤처질 자리**를 만들고,
프레임률에 따라 뒤처지는 양이 달라진다. 실측 편차가 **0.0000 px** 인 것이 그 값이다.

같은 이유로 `position_smoothing` 을 끈다. 부드럽게 따라오는 화면은 예쁘지만
**「같은 입력 = 같은 화면」이 아니다.** 켜 보면 최대 편차가 **82.10 px** 로 벌어진다(아래 표).

### 줌은 **1 이다** — 해상도가 시야 이득이 되면 안 된다

창이 큰 사람이 더 멀리 보면 그건 설정이 아니라 **유리함**이다 (BACKLOG P1 「줌 없음」).
그래서 두 군데서 막는다: 씬에 박힌 글자(`test_camera_zoom_is_one`)와
**실행 중의 캔버스 변환**(`measure_camera.gd` · `measure_view.gd`).

### 사라진 값 — `tile_offset`

바퀴 7 의 `tile_offset = (118, 123)` 이 없어졌다. **화면 칸 = 월드 칸**이다 (7절).
`WorldCollide.solid_from_seed` 의 이동량 인자와 `main.gd` 의 `screen_of()` 도 같이 지웠다.

임시 격자도 **월드에 고정**으로 바꿨다(`main.gd` 의 `visible_world_rect()`).
화면에 고정돼 있으면 **카메라가 따라가는지 사람 눈에 안 보인다** — 플레이어가 격자 위에서
얼어붙은 것처럼 보인다. `SHOT 960x540 · 색 4개 · 가장 넓은 한 색 50.0%`.

### 실측 게이트 `tools/tests/measure_camera.gd` (2026-09-14 실측)

`check.sh tests` 가 `COLLIDE` 뒤에 부른다. 헤드리스로 된다 — 캔버스 변환은 창 없이도 돈다.
네 방향으로 0.5초씩 걸으며 **매 프레임** 잰다.

```
CAMERA 스폰 월드 (6168.0, 6168.0) · 화면 (480.0, 270.0) · 보이는 월드 [P: (5688.0, 5898.0), S: (960.0, 540.0)]
CAMERA 최대 편차 0.0000 px · 걸은 거리 484.00 px · 줌 1.00x · 보이는 칸 20.00 x 11.25 · 표본 294
CAMERA ok (구간 4 × 0.5 s · 허용 편차 0.50 px · 물리 60 Hz)
```

- **판정은 「화면 안에 있나」가 아니라 「중심에서 몇 px 벗어났나」다.** 카메라가 아예 없어도
  플레이어가 화면 밖으로 나가는 데는 시간이 걸린다 — 느슨한 판정은 그 사이를 놓친다.
- **걸은 거리(484.00 px)를 같이 잰다.** 안 움직이면 「늘 한가운데」는 공짜다.
- 허용 편차 **0.50 px** 는 넉넉한 값이 아니라 **0 이어야 하는 값에 준 여유**다.
  끌려오는 카메라는 첫 표본이 이미 4.00 px 로 잡힌다(아래 ③).

### 대조군 — P1-6 으로 새로 넣은 3종 (2026-09-14 실측)

| 일부러 깨뜨린 것 | 단위 검사 57개 | 카메라 실측 |
|---|---|---|
| ① 카메라를 꺼 버린다 (`enabled = false`) | **전부 초록** | **최대 편차 8363.56 px** — 화면이 월드 원점을 비춘다 ✅ |
| ② 실행 중에 줌을 건다 (`player.gd` 가 `$Camera.zoom = (2,2)`) | **전부 초록** | **보이는 칸 10.00 x 5.62** — 시야가 2배 ✅ |
| ③ 부드럽게 따라오게 켠다 (`position_smoothing_speed = 2.0`) | **전부 초록** | **최대 편차 82.10 px** — 걷는 동안 화면이 뒤처진다 ✅ |

**셋 다 단위 검사 57개를 전부 초록으로 남긴다** — 씬에 박힌 글자(`zoom = 1`, 부드럽게
따라가기 끔)는 그대로고 **실행 중의 화면만** 달라지기 때문이다. 바퀴 3(속도)·4(화면)·
5(방향)·6(월드)·7(충돌)과 **여섯 번째 같은 모양의 구멍**이고, 여섯 번 다 실측이 잡았다.

전체: `REDTEAM 24 잡음, 1 놓침`. 놓친 하나는 게이트가 아니라 **바닥**이다 —
3절의 「검사를 지우면 바닥이 잡는다」(검사 57 · `mintests` 바닥 53).
무장된 `criteria.tsv` 안에 있어 **세션은 안 건드렸다**.

---

## 9. 그리기 (2026-09-14 실측)

**조건: 씨앗 20260914 · 창 1920×1080 · 논리 960×540 · 줌 1 · 60Hz · gl_compatibility**

임시 격자를 걷어냈다. 화면에 있는 것은 **진짜 월드**다 — 파랗게 보이는 칸이 곧
`WorldCollide` 가 막는 칸이다.

| 값 | 무엇 |
|---|---|
| **273 칸** | 한 화면에 걸리는 칸 수 (21 × 13). 어긋난 화면의 **최대**다 |
| **252 칸** | 칸 경계에 딱 맞춘 화면 (21 × 12). 960/48 = 20 은 떨어지고 540/48 = 11.25 는 안 떨어진다 |
| **65536 칸** | 월드 전체. **240배다** — 통째로 그려도 사람 눈에는 똑같이 보인다 |
| **6.71 µs** | 한 칸의 색을 구하는 비용 (잡음을 두 번 돈다: `tile_at` + `height_at`) |
| **1.83 ms** | 273칸 한 판 = **60Hz 한 프레임 예산 16.67 ms 의 11.0%** |

### 색이 곧 지형이다 — 덧칠이 아니다

물이냐 땅이냐는 **`WorldGen.tile_at` 이 정한다.** `WorldView` 가 `h > SEA_LEVEL` 을
다시 쓰지 않는 이유가 이것이다 — 두 벌을 두면 언젠가 한쪽만 고쳐져 **보이는 땅에
못 서는 날**이 온다. 그 안에서의 명암만 높이로 섞는다.

**흔들기는 세 채널에 같은 양을 더한다**(`TINT` ±0.035 = 8비트로 ±9). 그래서
**물은 언제나 b > g > r · 땅은 언제나 g > r > b** 이고, 이 부등식을 `test_world_view.gd`
가 7396칸으로 잰다. 곱하거나 채널마다 다르게 흔들면 **물처럼 보이는 땅**이 생긴다.

`DEEP_AT = -0.25` 는 고른 값이 아니라 **계산된 값**이다: 월드 밖은 감쇠가 1.25 로
포화하므로 높이가 `0.35 + 0.65 - 1.25 = -0.25` 를 넘을 수 없다 → **월드 밖은 자동으로
정확히 `DEEP`** 이고 분기가 필요 없다 (6절의 「섬 모양은 부등식이 보장한다」와 같은 수법).

### 화면이 단색이면 안 된다 — 칸마다 흔드는 진짜 이유

| | 색 수 | 가장 넓은 한 색 |
|---|---|---|
| 임시 격자 (바퀴 8) | 3개 | **50.0%** |
| 진짜 월드 (바퀴 9) | **217개** | **1.3%** |

흔들기가 없으면 스폰 주변은 전부 풀밭이라 한 화면이 거의 단색이 되고,
`shot.sh --max-flat` 이 **아무것도 안 잡는 공허한 상한**이 된다.
48px 격자가 눈에 남는 것은 덤이 아니라 필요다 — 240 px/s 가 5칸/초로 보이는지
**사람이 걸어서** 확인하는 유일한 방법이다.

### 색 캐시 — 프레임마다 다시 계산하지 않는다

1.83 ms 는 나무도 몹도 UI 도 없는 지금의 11% 다. 그래서 **칸이 아니라 보이는 범위가
바뀔 때만** 다시 채운다(`main.gd` 의 `_cache` / `_cache_range`).
**144 px(3칸) 걷는 동안 5번** 찼다 — 240 px/s 면 한 축당 초당 5번이라 평균 20배 싸다.

**대가는 상해도 눈에 안 보이는 것이다** — 캐시가 안 버려지면 화면이 월드에서 미끄러진다.
아래 대조군 ④ 가 **최대 색차 86.0/255** 로 잡는다.

### 실측 게이트 `tools/tests/measure_draw.gd` (2026-09-14 실측)

`check.sh tests` 가 `CAMERA` 뒤에 부른다. **`--headless` 로는 못 쓴다** —
렌더러가 더미라 뷰포트 텍스처가 빈다 (3b절).

구운 화면의 점 하나하나를 **캔버스 변환으로 월드 좌표로 되돌려** 그 칸의 색과 맞춘다.
스폰은 섬 한가운데라 전부 풀밭이므로 **+x 로 걸어 첫 바다를 찾아 그 경계에 세운다.**

```
DRAW 해안 칸 (229, 128) · 플레이어 (11016.0, 6168.0)
DRAW [서서]       표본 7844 · 불일치 0 · 최대 색차 0.8/255 · 물 45.9% · 땅 54.1% · 그린 칸 273 · 채운 횟수 2
DRAW [걷고 144 px] 표본 7844 · 불일치 0 · 최대 색차 0.8/255 · 물 31.0% · 땅 69.0% · 그린 칸 273 · 채운 횟수 5
DRAW ok (표본 간격 8 px · 중심 56 px 제외 · 서서 + 걷고 두 번)
```

- **최대 색차 0.8/255.** gl_compatibility 가 반올림하는 만큼이다 — 허용치 2/255 는
  그 자리를 남긴 값이지 느슨한 값이 아니다 (틀린 칸은 통째로 다른 색이라 수십/255 다).
- **물 45.9% / 땅 54.1%** 을 같이 잰다. 한 지형만 화면에 있으면 색 판정이 공허하다.
- **그린 칸 273 을 같이 읽는다** — 월드를 통째로 그려도 **픽셀은 한 점도 안 달라진다.**
- 걸으면 물이 45.9% → 31.0% 로 준다. 섬 안쪽으로 갔다는 뜻이다.

### 대조군 — 바퀴 9 로 새로 넣은 4종 (2026-09-14 실측)

| 일부러 깨뜨린 것 | 단위 검사 66개 | 그리기 실측 |
|---|---|---|
| ① 아무것도 안 그린다 (`draw_rect` 한 줄 삭제) | **전부 초록** | **7844 / 7844 점 어긋남 · 최대 색차 75.4/255** — 화면이 배경색(`4c4c4c`)뿐 ✅ |
| ② 다른 씨앗으로 그린다 (`WORLD_SEED + 1`) | **전부 초록** | **7844 / 7844 점 · 최대 색차 121.3/255** — 칸 (219,122) 이 `1e4e7e`(물) 인데 기대는 `778b4e`(땅) ✅ |
| ③ 월드를 통째로 그린다 (`Rect2i(0,0,256,256)`) | **전부 초록** | **픽셀은 7844 점 전부 일치 · 색차 0.8/255** — 그린 칸 **273 → 65536** 만 잡는다 ✅ |
| ④ 색 캐시를 안 버린다 (`if _cache.is_empty()`) | **전부 초록** | **7844 / 7844 점 · 최대 색차 86.0/255** · 채운 횟수 5 → **1** ✅ |

**넷 다 단위 검사 66개를 전부 초록으로 남긴다** — `WorldView` 의 순수 계산은 멀쩡하고
`main.gd` 가 그걸 쓰는 방식만 망가지기 때문이다. 바퀴 3(속도)·4(화면)·5(방향)·
6(월드)·7(충돌)·8(카메라)과 **일곱 번째 같은 모양의 구멍**이다.

**②와 ③이 이 게이트의 존재 이유다.** ②는 화면만 봐서는 절대 못 찾는다 — 멀쩡한 섬이
보이고, 파란 칸을 걸어 다니고 풀밭에서 막힐 뿐이다. ③은 픽셀을 아무리 정밀하게 재도
못 잡는다 — **세어 둔 숫자를 읽어야 한다.**

### 걷는 구간은 아직 대조군이 없다 — **놓친 자리로 적어 둔다**

게이트는 두 번 재지만 **넷 다 `[서서]` 에서 이미 빨개진다.** 게이트가 해안까지
**순간이동**시키는데, 그 순간이동 자체가 이미 카메라를 옮겨 캐시를 버리게 만들기
때문이다. 즉 `[걷고]` 구간이 **혼자 잡는 것은 지금 하나도 없다.**

지금 그 구간이 더하는 것은 대조군이 아니라 표본이다: 채운 횟수가 **2 → 5** 로 늘고
(물리가 민 진짜 재충전 3번), 물 비율이 **45.9% → 31.0%** 로 바뀐 화면에서 7844 점이
다시 전부 맞는다. **그것으로 충분한지는 대조군이 나와야 안다** — BACKLOG 에 넘겼다.
「걸어야만 깨지는 것」을 한 줄로 주입할 방법을 아직 못 찾았다.

전체: `REDTEAM 28 잡음, 1 놓침`. 놓친 하나는 게이트가 아니라 **사람의 마우스**다 —
「원복하면 다시 초록」에서 `FACE` 가 `91.73°`·`-92.55°`(기대 90°·-90°)로 튀었다.
`measure_facing.gd` 는 **진짜 커서를 뺏어** 재므로 사람이 마우스를 건드리면 빨개진다.
곧바로 다시 돌린 계약은 `ALL GREEN` 이다 (바퀴 8 과 **같은 자리에서 두 번째**).

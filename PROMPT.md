# 한 바퀴 지시서

> **세션이 매 바퀴 통째로 읽는 유일한 문서다. 부풀리지 마라** —
> 여기가 길어지면 매 바퀴의 비용이 계속 늘어난다.

## 1. 읽기

| 순서 | 파일 | 얼마나 |
|---|---|---|
| 1 | `BACKLOG.md` | **번호가 가장 작은 미완료 항목 하나만.** 전체를 읽지 마라 |
| 2 | `.loop/state.md` | 전체 (지난 바퀴들 — 문맥이 끊겨도 여기서 이어붙인다) |
| 3 | `NUMBERS.md` | 쓸 값이 있을 때만 그 절 |
| 4 | `GDD.md` | **그 영역을 처음 만들 때만** 해당 절. 매 바퀴 읽지 마라 |

**`[ASK]` 가 붙은 항목이면 여기서 멈춘다.** 구현하지 않고, 무엇을 정해야 하는지
`.loop/state.md` 에 적고 사람을 부른다. 사람이 답을 아는 것은 세션을 몇 번 열어도
같은 결론이 나온다 — 1판에서 그런 항목 하나가 세 바퀴를 돌며 $19 를 태웠다.

## 2. 만들기

**그 항목 하나만 만든다.** 눈에 보이는 다른 문제는 고치지 말고 `BACKLOG.md` 아래에
한 줄로 넘긴다.

## 3. 자체 QA — **직접 돌려보고 확인한다**

```
bash tools/loop/run-contract.sh
```

- **이것만이 `.loop/results.json` 을 쓴다.** 초록/빨강의 판정은 여기서 나온다.
- 화면이 필요한 것은 실제로 띄워서 본다: `tools/loop/godot.sh 5 -- --path .`
- **부족하거나 개선할 것이 보이면 2번으로 돌아간다.** 단, 아래 정지 규칙에 걸리면 멈춘다.
- **새 검사를 만들었으면 대조군을 돌린다** — 일부러 깨뜨려서 빨개지는지 확인하고,
  그 결과를 적는다. 안 하면 아무것도 안 잡는 가짜 게이트가 쌓인다.

## 4. 커밋

초록일 때만 커밋한다. **경로를 지정해서** 커밋한다 (`git add -A` 금지).
커밋 메시지에 **잰 값**을 적는다.

그리고 `.loop/state.md` 를 갱신한다 — 바퀴 번호 · 만든 것 · **잰 값** · 다음.
항목이 끝났으면 `BACKLOG.md` 에서 `- [x]` 로 표시한다.

---

## 정지 규칙 (하나라도 걸리면 즉시 멈추고 사람을 부른다)

1. **ALL GREEN** — 할 일이 끝났다
2. **`[ASK]` 항목** — 사람이 답해야 한다
3. **같은 실패 두 바퀴 연속** — 고치는 게 아니라 찍고 있다
4. **회귀** — 통과하던 것이 깨졌다
5. **3번 안에서 같은 항목을 3회 반복했는데 실패 수가 안 줄었다** — 과제가 크다. 쪼개기를 제안한다
6. **능력 경계** — 외부 의존 · 환경 · 사람의 눈이 필요한 판단

## Red lines

- **채점자 보고 없이 성공을 말하지 않는다.** 「됐습니다」는 증거가 아니다.
- **검사를 약하게 하거나 지워서 초록에 도달하지 않는다.**
  `.loop/criteria.tsv` 는 무장 중에는 해시로 잠긴다 — 고치면 `exit 77` 로 죽는다.
- **잰 값과 조건을 같이 적는다** — `TESTS 8 passed` 가 아니라
  `TESTS 8 passed, 0 failed · 창 1920x1080 · 배율 2.00x`.
- **`NUMBERS.md` 의 값을 고치려면 새로 재고 조건을 같이 적는다.**
- 루프의 산출물은 **사람이 볼 제안**이지 확정된 사실이 아니다. 끝에 diff 를 보인다.

## 바꿔도 되는 곳 / 안 되는 곳

| | |
|---|---|
| 바꿔도 됨 | `scenes/` `scripts/` `assets/` `tools/` `project.godot` · `.loop/state.md` · `BACKLOG.md` |
| **안 됨** | `.loop/criteria.tsv` (무장 중 잠김) · `.loop/results.json` (기계만 쓴다) · `NUMBERS.md` (재지 않고 고치지 않는다) · `GDD.md` (사람이 정한다) |
